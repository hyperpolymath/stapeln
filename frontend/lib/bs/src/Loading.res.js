// Generated by ReScript, PLEASE EDIT WITH CARE

import * as JsxRuntime from "react/jsx-runtime";

function spinner(sizeOpt, colorOpt, labelOpt) {
  let size = sizeOpt !== undefined ? sizeOpt : "default";
  let color = colorOpt !== undefined ? colorOpt : "#0052CC";
  let label = labelOpt !== undefined ? labelOpt : "Loading...";
  let spinnerSize;
  switch (size) {
    case "large" :
      spinnerSize = "64px";
      break;
    case "small" :
      spinnerSize = "24px";
      break;
    default:
      spinnerSize = "40px";
  }
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("div", {
        style: {
          border: "4px solid #E2E8F0",
          borderTop: "4px solid " + color,
          height: spinnerSize,
          width: spinnerSize,
          borderRadius: "50%",
          animation: "spin 0.8s linear infinite"
        }
      }),
      JsxRuntime.jsx("span", {
        children: label,
        style: {
          height: "1px",
          left: "-10000px",
          overflow: "hidden",
          position: "absolute",
          width: "1px"
        }
      })
    ],
    "aria-label": label,
    "aria-live": "polite",
    role: "status",
    style: {
      display: "flex",
      padding: "2rem",
      alignItems: "center",
      justifyContent: "center"
    }
  });
}

function skeleton(widthOpt, heightOpt, borderRadiusOpt, marginBottomOpt) {
  let width = widthOpt !== undefined ? widthOpt : "100%";
  let height = heightOpt !== undefined ? heightOpt : "20px";
  let borderRadius = borderRadiusOpt !== undefined ? borderRadiusOpt : "4px";
  let marginBottom = marginBottomOpt !== undefined ? marginBottomOpt : "0.5rem";
  return JsxRuntime.jsx("div", {
    "aria-label": "Loading content...",
    role: "status",
    style: {
      backgroundColor: "#E2E8F0",
      height: height,
      marginBottom: marginBottom,
      width: width,
      borderRadius: borderRadius,
      animation: "pulse 1.5s ease-in-out infinite"
    }
  });
}

function skeletonListItem() {
  return JsxRuntime.jsxs("div", {
    children: [
      skeleton("60%", "16px", undefined, "0.75rem"),
      skeleton("100%", "14px", undefined, "0.5rem"),
      skeleton("80%", "14px", undefined, "0")
    ],
    style: {
      border: "1px solid #E2E8F0",
      marginBottom: "0.5rem",
      padding: "1rem",
      borderRadius: "8px"
    }
  });
}

function overlay(messageOpt, isDarkOpt) {
  let message = messageOpt !== undefined ? messageOpt : "Loading...";
  let isDark = isDarkOpt !== undefined ? isDarkOpt : false;
  return JsxRuntime.jsxs("div", {
    children: [
      spinner("large", isDark ? "#66B2FF" : "#0052CC", message),
      JsxRuntime.jsx("p", {
        children: message,
        style: {
          fontSize: "1.1rem",
          fontWeight: "600",
          marginTop: "1rem"
        }
      })
    ],
    "aria-label": message,
    "aria-live": "polite",
    role: "status",
    style: {
      backgroundColor: isDark ? "rgba(0, 0, 0, 0.8)" : "rgba(255, 255, 255, 0.9)",
      bottom: "0",
      color: isDark ? "#FFFFFF" : "#000000",
      display: "flex",
      left: "0",
      position: "fixed",
      right: "0",
      top: "0",
      zIndex: "9999",
      alignItems: "center",
      flexDirection: "column",
      justifyContent: "center"
    }
  });
}

function wrapper(isLoading, children, fallback, sizeOpt) {
  let size = sizeOpt !== undefined ? sizeOpt : "default";
  if (isLoading) {
    if (fallback !== undefined) {
      return fallback;
    } else {
      return spinner(size, undefined, undefined);
    }
  } else {
    return children;
  }
}

export {
  spinner,
  skeleton,
  skeletonListItem,
  overlay,
  wrapper,
}
/* react/jsx-runtime Not a pure module */
