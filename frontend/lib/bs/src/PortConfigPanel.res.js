// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Belt_Int from "@rescript/runtime/lib/es6/Belt_Int.js";
import * as ApiClient from "./ApiClient.res.js";
import * as Belt_Array from "@rescript/runtime/lib/es6/Belt_Array.js";
import * as JsxRuntime from "react/jsx-runtime";

let emptyPinholeForm = {
  source: "",
  destination: "",
  port: "",
  ttlSeconds: 300,
  reason: "",
  protocol: "tcp"
};

let init_ports = [
  {
    number: 22,
    protocol: "SSH",
    state: "Closed",
    risk: "Critical",
    description: "Secure Shell"
  },
  {
    number: 80,
    protocol: "HTTP",
    state: "Closed",
    risk: "Medium",
    description: "Web Server"
  },
  {
    number: 443,
    protocol: "HTTPS",
    state: "Closed",
    risk: "Medium",
    description: "Secure Web"
  },
  {
    number: 3306,
    protocol: "MySQL",
    state: "Closed",
    risk: "High",
    description: "MySQL Database"
  },
  {
    number: 5432,
    protocol: "PostgreSQL",
    state: "Closed",
    risk: "High",
    description: "PostgreSQL DB"
  },
  {
    number: 8080,
    protocol: "HTTP-Alt",
    state: "Closed",
    risk: "Safe",
    description: "Alt HTTP"
  },
  {
    number: 9000,
    protocol: "Custom",
    state: "Closed",
    risk: "Safe",
    description: "Application"
  }
];

let init_activePinholes = [];

let init = {
  ports: init_ports,
  selectedPort: undefined,
  showEphemeralConfig: false,
  ephemeralDuration: 300,
  ephemeralRemaining: undefined,
  showPinholeManager: false,
  pinholeForm: emptyPinholeForm,
  activePinholes: init_activePinholes,
  pinholeError: undefined,
  pinholeLoading: false
};

function update(msg, state) {
  if (typeof msg !== "object") {
    switch (msg) {
      case "ToggleEphemeralConfig" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: !state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "TogglePinholeManager" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: !state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "ClearPinholeError" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: undefined,
          pinholeLoading: state.pinholeLoading
        };
      case "DecrementPinholeTimers" :
        return state;
    }
  } else {
    switch (msg.TAG) {
      case "SelectPort" :
        return {
          ports: state.ports,
          selectedPort: msg._0,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPortState" :
        let newState = msg._1;
        let portNum = msg._0;
        let updatedPorts = Belt_Array.map(state.ports, port => {
          if (port.number === portNum) {
            return {
              number: port.number,
              protocol: port.protocol,
              state: newState,
              risk: port.risk,
              description: port.description
            };
          } else {
            return port;
          }
        });
        return {
          ports: updatedPorts,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetEphemeralDuration" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: msg._0,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "StartEphemeralTimer" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralDuration,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "DecrementEphemeralTimer" :
        let remaining = state.ephemeralRemaining;
        if (remaining !== undefined && remaining > 0) {
          return {
            ports: state.ports,
            selectedPort: state.selectedPort,
            showEphemeralConfig: state.showEphemeralConfig,
            ephemeralDuration: state.ephemeralDuration,
            ephemeralRemaining: remaining - 1 | 0,
            showPinholeManager: state.showPinholeManager,
            pinholeForm: state.pinholeForm,
            activePinholes: state.activePinholes,
            pinholeError: state.pinholeError,
            pinholeLoading: state.pinholeLoading
          };
        } else {
          return state;
        }
      case "ExpireEphemeralPort" :
        let portNum$1 = msg._0;
        let updatedPorts$1 = Belt_Array.map(state.ports, port => {
          if (port.number === portNum$1) {
            return {
              number: port.number,
              protocol: port.protocol,
              state: "Closed",
              risk: port.risk,
              description: port.description
            };
          } else {
            return port;
          }
        });
        return {
          ports: updatedPorts$1,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: undefined,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormSource" :
        let init = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: msg._0,
            destination: init.destination,
            port: init.port,
            ttlSeconds: init.ttlSeconds,
            reason: init.reason,
            protocol: init.protocol
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormDestination" :
        let init$1 = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: init$1.source,
            destination: msg._0,
            port: init$1.port,
            ttlSeconds: init$1.ttlSeconds,
            reason: init$1.reason,
            protocol: init$1.protocol
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormPort" :
        let init$2 = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: init$2.source,
            destination: init$2.destination,
            port: msg._0,
            ttlSeconds: init$2.ttlSeconds,
            reason: init$2.reason,
            protocol: init$2.protocol
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormTtl" :
        let init$3 = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: init$3.source,
            destination: init$3.destination,
            port: init$3.port,
            ttlSeconds: msg._0,
            reason: init$3.reason,
            protocol: init$3.protocol
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormReason" :
        let init$4 = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: init$4.source,
            destination: init$4.destination,
            port: init$4.port,
            ttlSeconds: init$4.ttlSeconds,
            reason: msg._0,
            protocol: init$4.protocol
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "SetPinholeFormProtocol" :
        let init$5 = state.pinholeForm;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: {
            source: init$5.source,
            destination: init$5.destination,
            port: init$5.port,
            ttlSeconds: init$5.ttlSeconds,
            reason: init$5.reason,
            protocol: msg._0
          },
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "PinholesLoaded" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: msg._0,
          pinholeError: state.pinholeError,
          pinholeLoading: false
        };
      case "PinholeCreated" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: emptyPinholeForm,
          activePinholes: Belt_Array.concat(state.activePinholes, [msg._0]),
          pinholeError: undefined,
          pinholeLoading: false
        };
      case "PinholeRevoked" :
        let id = msg._0;
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: Belt_Array.keep(state.activePinholes, p => p.id !== id),
          pinholeError: state.pinholeError,
          pinholeLoading: state.pinholeLoading
        };
      case "PinholeError" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: msg._0,
          pinholeLoading: false
        };
      case "SetPinholeLoading" :
        return {
          ports: state.ports,
          selectedPort: state.selectedPort,
          showEphemeralConfig: state.showEphemeralConfig,
          ephemeralDuration: state.ephemeralDuration,
          ephemeralRemaining: state.ephemeralRemaining,
          showPinholeManager: state.showPinholeManager,
          pinholeForm: state.pinholeForm,
          activePinholes: state.activePinholes,
          pinholeError: state.pinholeError,
          pinholeLoading: msg._0
        };
    }
  }
}

function riskIndicator(risk) {
  switch (risk) {
    case "Critical" :
      return "🔴";
    case "High" :
      return "🟠";
    case "Medium" :
      return "🟡";
    case "Safe" :
      return "✅";
  }
}

function riskColor(risk) {
  switch (risk) {
    case "Critical" :
      return "#f44336";
    case "High" :
      return "#ff9800";
    case "Medium" :
      return "#ffc107";
    case "Safe" :
      return "#4caf50";
  }
}

function formatDuration(seconds) {
  if (seconds < 60) {
    return String(seconds) + "s";
  } else if (seconds < 3600) {
    return String(seconds / 60 | 0) + "m";
  } else if (seconds < 86400) {
    return String(seconds / 3600 | 0) + "h";
  } else {
    return String(seconds / 86400 | 0) + "d";
  }
}

function formatCountdown(seconds) {
  let mins = seconds / 60 | 0;
  let secs = seconds % 60;
  if (mins > 0) {
    return String(mins) + "m " + String(secs) + "s";
  } else {
    return String(secs) + "s";
  }
}

function stateLabel(state) {
  if (typeof state !== "object") {
    if (state === "Closed") {
      return "Closed";
    } else {
      return "Open";
    }
  } else {
    return "Ephemeral (" + formatDuration(state._0) + ")";
  }
}

function stateColor(state) {
  if (typeof state !== "object") {
    if (state === "Closed") {
      return "#4caf50";
    } else {
      return "#f44336";
    }
  } else {
    return "#ff9800";
  }
}

function parsePinhole(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return;
  }
  let getString = key => {
    let v = json[key];
    if (typeof v === "string") {
      return v;
    }
  };
  let getInt = key => {
    let v = json[key];
    if (typeof v === "number") {
      return v | 0;
    }
  };
  let match = getString("id");
  let match$1 = getString("source");
  let match$2 = getString("destination");
  let match$3 = getInt("port");
  let match$4 = getString("protocol");
  let match$5 = getInt("ttl_seconds");
  let match$6 = getString("created_at");
  let match$7 = getString("expires_at");
  let match$8 = getString("status");
  let match$9 = getString("reason");
  if (match !== undefined && match$1 !== undefined && match$2 !== undefined && match$3 !== undefined && match$4 !== undefined && match$5 !== undefined && match$6 !== undefined && match$7 !== undefined && match$8 !== undefined && match$9 !== undefined) {
    return {
      id: match,
      source: match$1,
      destination: match$2,
      port: match$3,
      protocol: match$4,
      ttlSeconds: match$5,
      createdAt: match$6,
      expiresAt: match$7,
      status: match$8,
      reason: match$9
    };
  }
}

function parsePinholesResponse(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return [];
  }
  let arr = json["pinholes"];
  if (arr !== undefined) {
    if (Array.isArray(arr)) {
      return Belt_Array.keepMap(arr, parsePinhole);
    } else {
      return [];
    }
  } else {
    return [];
  }
}

function parsePinholeResponse(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return;
  }
  let p = json["pinhole"];
  if (p !== undefined) {
    return parsePinhole(p);
  }
}

function viewPortRow(port, dispatch) {
  let match = port.state;
  let tmp;
  tmp = typeof match !== "object" ? "#2a3142" : "#ff9800";
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("span", {
            children: riskIndicator(port.risk),
            style: {
              fontSize: "24px"
            }
          }),
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("div", {
                children: "Port " + String(port.number),
                style: {
                  color: "#e0e6ed",
                  fontSize: "16px",
                  fontWeight: "700"
                }
              }),
              JsxRuntime.jsx("div", {
                children: port.protocol + " - " + port.description,
                style: {
                  color: "#8892a6",
                  fontSize: "12px",
                  marginTop: "2px"
                }
              })
            ]
          })
        ],
        style: {
          alignItems: "center",
          display: "flex",
          gap: "16px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("button", {
            children: "Closed",
            style: {
              background: port.state === "Closed" ? "#4caf50" : "#2a3142",
              border: "none",
              borderRadius: "6px",
              color: "white",
              cursor: "pointer",
              fontSize: "12px",
              fontWeight: "600",
              padding: "8px 16px"
            },
            onClick: param => dispatch({
              TAG: "SetPortState",
              _0: port.number,
              _1: "Closed"
            })
          }),
          JsxRuntime.jsx("button", {
            children: "Ephemeral",
            style: {
              background: tmp,
              border: "none",
              borderRadius: "6px",
              color: "white",
              cursor: "pointer",
              fontSize: "12px",
              fontWeight: "600",
              padding: "8px 16px"
            },
            onClick: param => dispatch({
              TAG: "SetPortState",
              _0: port.number,
              _1: {
                TAG: "Ephemeral",
                _0: 300
              }
            })
          }),
          JsxRuntime.jsx("button", {
            children: "Open",
            style: {
              background: port.state === "Open" ? "#f44336" : "#2a3142",
              border: "none",
              borderRadius: "6px",
              color: "white",
              cursor: "pointer",
              fontSize: "12px",
              fontWeight: "600",
              padding: "8px 16px"
            },
            onClick: param => dispatch({
              TAG: "SetPortState",
              _0: port.number,
              _1: "Open"
            })
          })
        ],
        style: {
          display: "flex",
          gap: "8px"
        }
      }),
      JsxRuntime.jsx("div", {
        children: stateLabel(port.state),
        style: {
          background: stateColor(port.state),
          borderRadius: "6px",
          color: "white",
          fontSize: "11px",
          fontWeight: "700",
          minWidth: "80px",
          padding: "6px 12px",
          textAlign: "center"
        }
      })
    ],
    className: "port-row",
    style: {
      alignItems: "center",
      background: "linear-gradient(135deg, #1e2431 0%, #252d3d 100%)",
      border: "1px solid #2a3142",
      borderRadius: "12px",
      cursor: "pointer",
      display: "flex",
      justifyContent: "space-between",
      marginBottom: "8px",
      padding: "16px",
      transition: "all 0.2s"
    }
  }, String(port.number));
}

function viewEphemeralConfig(state, dispatch) {
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("h3", {
        children: "⏱️ Ephemeral Pinhole Configuration",
        style: {
          color: "#ff9800",
          fontSize: "18px",
          fontWeight: "700",
          marginBottom: "16px"
        }
      }),
      JsxRuntime.jsx("p", {
        children: "Ephemeral pinholes automatically close after a set duration. Perfect for temporary access (SSH debugging, database migrations, etc.).",
        style: {
          color: "#b0b8c4",
          fontSize: "13px",
          lineHeight: "1.6",
          marginBottom: "16px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("label", {
            children: "Duration:",
            style: {
              color: "#8892a6",
              display: "block",
              fontSize: "12px",
              marginBottom: "8px"
            }
          }),
          JsxRuntime.jsx("div", {
            children: Belt_Array.map([
              30,
              60,
              300,
              600,
              1800,
              3600,
              7200,
              14400,
              43200,
              86400
            ], seconds => JsxRuntime.jsx("button", {
              children: formatDuration(seconds),
              style: {
                background: state.ephemeralDuration === seconds ? "#4a9eff" : "#2a3142",
                border: "none",
                borderRadius: "6px",
                color: "white",
                cursor: "pointer",
                fontSize: "12px",
                fontWeight: "600",
                padding: "8px 16px"
              },
              onClick: param => dispatch({
                TAG: "SetEphemeralDuration",
                _0: seconds
              })
            }, String(seconds))),
            style: {
              display: "flex",
              flexWrap: "wrap",
              gap: "8px"
            }
          })
        ],
        style: {
          marginBottom: "16px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("strong", {
            children: "Security Note:"
          }),
          " Ephemeral ports automatically close after the timer expires. All access is logged to VeriSimDB for audit."
        ],
        style: {
          background: "rgba(255, 152, 0, 0.1)",
          border: "1px solid #ff9800",
          borderRadius: "6px",
          color: "#ff9800",
          fontSize: "12px",
          padding: "12px"
        }
      })
    ],
    className: "ephemeral-config",
    style: {
      background: "#1e2431",
      border: "2px solid #ff9800",
      borderRadius: "12px",
      marginTop: "16px",
      padding: "24px"
    }
  });
}

let inputStyle = {
  background: "#1a1f2e",
  border: "1px solid #2a3142",
  borderRadius: "6px",
  color: "#e0e6ed",
  fontSize: "13px",
  padding: "10px 14px",
  width: "100%"
};

let labelStyle = {
  color: "#8892a6",
  display: "block",
  fontSize: "12px",
  fontWeight: "600",
  marginBottom: "6px"
};

function viewPinholeForm(state, dispatch, onSubmit) {
  let form = state.pinholeForm;
  let err = state.pinholeError;
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("h4", {
        children: "Create Ephemeral Pinhole",
        style: {
          color: "#4a9eff",
          fontSize: "16px",
          fontWeight: "700",
          marginBottom: "20px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("label", {
                children: "Source Service:",
                style: labelStyle
              }),
              JsxRuntime.jsx("input", {
                style: inputStyle,
                placeholder: "e.g. web-app",
                type: "text",
                value: form.source,
                onChange: e => dispatch({
                  TAG: "SetPinholeFormSource",
                  _0: e.target.value
                })
              })
            ]
          }),
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("label", {
                children: "Destination Service:",
                style: labelStyle
              }),
              JsxRuntime.jsx("input", {
                style: inputStyle,
                placeholder: "e.g. postgres-primary",
                type: "text",
                value: form.destination,
                onChange: e => dispatch({
                  TAG: "SetPinholeFormDestination",
                  _0: e.target.value
                })
              })
            ]
          }),
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("label", {
                children: "Port:",
                style: labelStyle
              }),
              JsxRuntime.jsx("input", {
                style: inputStyle,
                placeholder: "e.g. 5432",
                type: "number",
                value: form.port,
                onChange: e => dispatch({
                  TAG: "SetPinholeFormPort",
                  _0: e.target.value
                })
              })
            ]
          }),
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("label", {
                children: "Protocol:",
                style: labelStyle
              }),
              JsxRuntime.jsxs("select", {
                children: [
                  JsxRuntime.jsx("option", {
                    children: "TCP",
                    value: "tcp"
                  }),
                  JsxRuntime.jsx("option", {
                    children: "UDP",
                    value: "udp"
                  })
                ],
                style: inputStyle,
                value: form.protocol,
                onChange: e => dispatch({
                  TAG: "SetPinholeFormProtocol",
                  _0: e.target.value
                })
              })
            ]
          })
        ],
        style: {
          display: "grid",
          gap: "16px",
          gridTemplateColumns: "1fr 1fr",
          marginBottom: "16px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("label", {
            children: "Reason:",
            style: labelStyle
          }),
          JsxRuntime.jsx("input", {
            style: inputStyle,
            placeholder: "e.g. Database migration, SSH debugging session",
            type: "text",
            value: form.reason,
            onChange: e => dispatch({
              TAG: "SetPinholeFormReason",
              _0: e.target.value
            })
          })
        ],
        style: {
          marginBottom: "16px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("label", {
            children: "Time-to-Live:",
            style: labelStyle
          }),
          JsxRuntime.jsx("div", {
            children: Belt_Array.map([
              60,
              300,
              600,
              1800,
              3600,
              7200,
              14400,
              86400
            ], seconds => JsxRuntime.jsx("button", {
              children: formatDuration(seconds),
              style: {
                background: form.ttlSeconds === seconds ? "#4a9eff" : "#2a3142",
                border: "none",
                borderRadius: "6px",
                color: "white",
                cursor: "pointer",
                fontSize: "12px",
                fontWeight: "600",
                padding: "8px 16px"
              },
              onClick: param => dispatch({
                TAG: "SetPinholeFormTtl",
                _0: seconds
              })
            }, String(seconds))),
            style: {
              display: "flex",
              flexWrap: "wrap",
              gap: "8px"
            }
          })
        ],
        style: {
          marginBottom: "20px"
        }
      }),
      err !== undefined ? JsxRuntime.jsx("div", {
          children: err,
          style: {
            background: "rgba(244, 67, 54, 0.15)",
            border: "1px solid #f44336",
            borderRadius: "6px",
            color: "#f44336",
            fontSize: "12px",
            marginBottom: "16px",
            padding: "10px 14px"
          }
        }) : null,
      JsxRuntime.jsx("button", {
        children: state.pinholeLoading ? "Creating..." : "Create Pinhole",
        style: {
          background: state.pinholeLoading ? "#555" : "linear-gradient(135deg, #4a9eff, #357abd)",
          border: "none",
          borderRadius: "8px",
          color: "white",
          cursor: state.pinholeLoading ? "not-allowed" : "pointer",
          fontSize: "14px",
          fontWeight: "600",
          padding: "12px 24px"
        },
        disabled: state.pinholeLoading || form.source === "" || form.destination === "" || form.port === "" || form.reason === "",
        onClick: param => onSubmit()
      })
    ],
    style: {
      background: "#1e2431",
      border: "1px solid #2a3142",
      borderRadius: "12px",
      marginBottom: "24px",
      padding: "24px"
    }
  });
}

function viewPinholeRow(p, onRevoke) {
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("span", {
            children: "🔓",
            style: {
              fontSize: "20px"
            }
          }),
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("div", {
                children: p.source + " -> " + p.destination + ":" + String(p.port) + "/" + p.protocol,
                style: {
                  color: "#e0e6ed",
                  fontSize: "14px",
                  fontWeight: "700"
                }
              }),
              JsxRuntime.jsx("div", {
                children: p.reason,
                style: {
                  color: "#8892a6",
                  fontSize: "11px",
                  marginTop: "2px"
                }
              })
            ]
          })
        ],
        style: {
          alignItems: "center",
          display: "flex",
          gap: "14px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsxs("div", {
                children: [
                  "TTL: ",
                  formatDuration(p.ttlSeconds)
                ],
                style: {
                  color: "#ff9800",
                  fontSize: "12px",
                  fontWeight: "600"
                }
              }),
              JsxRuntime.jsxs("div", {
                children: [
                  "Expires: ",
                  p.expiresAt
                ],
                style: {
                  color: "#8892a6",
                  fontSize: "10px",
                  marginTop: "2px"
                }
              })
            ],
            style: {
              textAlign: "right"
            }
          }),
          JsxRuntime.jsx("button", {
            children: "Revoke",
            style: {
              background: "rgba(244, 67, 54, 0.2)",
              border: "1px solid #f44336",
              borderRadius: "6px",
              color: "#f44336",
              cursor: "pointer",
              fontSize: "11px",
              fontWeight: "700",
              padding: "8px 14px"
            },
            onClick: param => onRevoke(p.id)
          })
        ],
        style: {
          alignItems: "center",
          display: "flex",
          gap: "12px"
        }
      })
    ],
    style: {
      alignItems: "center",
      background: "linear-gradient(135deg, #1e2431 0%, #252d3d 100%)",
      border: "1px solid #ff9800",
      borderRadius: "10px",
      display: "flex",
      justifyContent: "space-between",
      marginBottom: "8px",
      padding: "14px 16px",
      transition: "all 0.2s"
    }
  }, p.id);
}

function viewActivePinholes(pinholes, onRevoke) {
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("h4", {
        children: [
          "Active Pinholes (",
          String(pinholes.length),
          ")"
        ],
        style: {
          color: "#ff9800",
          fontSize: "16px",
          fontWeight: "700",
          marginBottom: "16px"
        }
      }),
      pinholes.length === 0 ? JsxRuntime.jsx("div", {
          children: "No active pinholes. Create one above to grant temporary network access.",
          style: {
            background: "#1e2431",
            border: "1px dashed #2a3142",
            borderRadius: "10px",
            color: "#555e6e",
            fontSize: "13px",
            padding: "24px",
            textAlign: "center"
          }
        }) : JsxRuntime.jsx("div", {
          children: Belt_Array.map(pinholes, p => viewPinholeRow(p, onRevoke))
        })
    ],
    style: {
      marginBottom: "24px"
    }
  });
}

function PortConfigPanel(props) {
  let onStateChange = props.onStateChange;
  let initialState = props.initialState;
  let match = React.useState(() => {
    if (initialState !== undefined) {
      return initialState;
    } else {
      return init;
    }
  });
  let setState = match[1];
  let state = match[0];
  let dispatch = msg => {
    let newState = update(msg, state);
    setState(param => newState);
    if (onStateChange !== undefined) {
      return onStateChange(newState);
    }
  };
  React.useEffect(() => {
    if (state.showPinholeManager) {
      dispatch({
        TAG: "SetPinholeLoading",
        _0: true
      });
      ApiClient.listPinholes().then(result => {
        if (result.TAG === "Ok") {
          let pinholes = parsePinholesResponse(result._0);
          dispatch({
            TAG: "PinholesLoaded",
            _0: pinholes
          });
        } else {
          dispatch({
            TAG: "PinholeError",
            _0: result._0
          });
        }
        return Promise.resolve();
      });
    }
  }, [state.showPinholeManager]);
  React.useEffect(() => {
    let remaining = state.ephemeralRemaining;
    if (remaining === undefined) {
      return;
    }
    if (remaining <= 0) {
      if (remaining !== 0) {
        return;
      } else {
        Belt_Array.forEach(state.ports, port => {
          let match = port.state;
          if (typeof match !== "object") {
            return;
          } else {
            return dispatch({
              TAG: "ExpireEphemeralPort",
              _0: port.number
            });
          }
        });
        return;
      }
    }
    let timeoutId = setTimeout(() => dispatch({
      TAG: "DecrementEphemeralTimer",
      _0: 0
    }), 1000);
    return () => {
      clearTimeout(timeoutId);
    };
  }, [state.ephemeralRemaining]);
  let handleCreatePinhole = () => {
    let form = state.pinholeForm;
    let portNum = Belt_Int.fromString(form.port);
    if (portNum !== undefined && portNum > 0 && portNum <= 65535) {
      dispatch({
        TAG: "SetPinholeLoading",
        _0: true
      });
      ApiClient.createPinhole(form.source, form.destination, portNum, form.ttlSeconds, form.reason, form.protocol).then(result => {
        if (result.TAG === "Ok") {
          let p = parsePinholeResponse(result._0);
          if (p !== undefined) {
            dispatch({
              TAG: "PinholeCreated",
              _0: p
            });
          } else {
            dispatch({
              TAG: "PinholeError",
              _0: "Failed to parse pinhole response"
            });
          }
        } else {
          dispatch({
            TAG: "PinholeError",
            _0: result._0
          });
        }
        return Promise.resolve();
      });
      return;
    } else {
      return dispatch({
        TAG: "PinholeError",
        _0: "Port must be a number between 1 and 65535"
      });
    }
  };
  let handleRevokePinhole = id => {
    ApiClient.revokePinhole(id).then(result => {
      if (result.TAG === "Ok") {
        dispatch({
          TAG: "PinholeRevoked",
          _0: id
        });
      } else {
        dispatch({
          TAG: "PinholeError",
          _0: result._0
        });
      }
      return Promise.resolve();
    });
  };
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("h1", {
            children: "🔌 Port Configuration",
            style: {
              background: "linear-gradient(135deg, #4a9eff, #7b6cff)",
              fontSize: "32px",
              fontWeight: "700",
              marginBottom: "8px"
            }
          }),
          JsxRuntime.jsx("p", {
            children: "Configure which ports are accessible and for how long",
            style: {
              color: "#8892a6",
              fontSize: "16px"
            }
          })
        ],
        style: {
          marginBottom: "32px"
        }
      }),
      JsxRuntime.jsx("div", {
        children: Belt_Array.map([
          [
            "Closed",
            Belt_Array.reduce(state.ports, 0, (acc, p) => {
              if (p.state === "Closed") {
                return acc + 1 | 0;
              } else {
                return acc;
              }
            }),
            "#4caf50"
          ],
          [
            "Open",
            Belt_Array.reduce(state.ports, 0, (acc, p) => {
              if (p.state === "Open") {
                return acc + 1 | 0;
              } else {
                return acc;
              }
            }),
            "#f44336"
          ],
          [
            "Ephemeral",
            Belt_Array.reduce(state.ports, 0, (acc, p) => {
              let match = p.state;
              if (typeof match !== "object") {
                return acc;
              } else {
                return acc + 1 | 0;
              }
            }),
            "#ff9800"
          ],
          [
            "Critical Risk",
            Belt_Array.reduce(state.ports, 0, (acc, p) => {
              if (p.risk === "Critical" && p.state !== "Closed") {
                return acc + 1 | 0;
              } else {
                return acc;
              }
            }),
            "#f44336"
          ]
        ], param => {
          let label = param[0];
          return JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("div", {
                children: String(param[1]),
                style: {
                  color: param[2],
                  fontSize: "36px",
                  fontWeight: "700",
                  marginBottom: "8px"
                }
              }),
              JsxRuntime.jsx("div", {
                children: label,
                style: {
                  color: "#8892a6",
                  fontSize: "13px"
                }
              })
            ],
            style: {
              background: "linear-gradient(135deg, #1e2431 0%, #252d3d 100%)",
              border: "2px solid #2a3142",
              borderRadius: "12px",
              padding: "20px",
              textAlign: "center"
            }
          }, label);
        }),
        style: {
          display: "grid",
          gap: "16px",
          gridTemplateColumns: "repeat(4, 1fr)",
          marginBottom: "32px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("button", {
            children: (
              state.showEphemeralConfig ? "Hide" : "Configure"
            ) + " Ephemeral Settings",
            style: {
              background: "linear-gradient(135deg, #ff9800, #f57c00)",
              border: "none",
              borderRadius: "8px",
              color: "white",
              cursor: "pointer",
              fontSize: "14px",
              fontWeight: "600",
              padding: "12px 24px"
            },
            onClick: param => dispatch("ToggleEphemeralConfig")
          }),
          JsxRuntime.jsx("button", {
            children: (
              state.showPinholeManager ? "Hide" : "Manage"
            ) + " Pinholes",
            style: {
              background: state.showPinholeManager ? "linear-gradient(135deg, #f57c00, #e65100)" : "linear-gradient(135deg, #7b6cff, #5a4fcf)",
              border: "none",
              borderRadius: "8px",
              color: "white",
              cursor: "pointer",
              fontSize: "14px",
              fontWeight: "600",
              padding: "12px 24px"
            },
            onClick: param => dispatch("TogglePinholeManager")
          })
        ],
        style: {
          display: "flex",
          gap: "12px",
          marginBottom: "16px"
        }
      }),
      state.showEphemeralConfig ? viewEphemeralConfig(state, dispatch) : null,
      state.showPinholeManager ? JsxRuntime.jsxs("div", {
          children: [
            JsxRuntime.jsx("h3", {
              children: "🔐 Ephemeral Pinhole Manager",
              style: {
                color: "#7b6cff",
                fontSize: "20px",
                fontWeight: "700",
                marginBottom: "20px"
              }
            }),
            JsxRuntime.jsx("p", {
              children: "Create temporary, scoped network access rules between services. Pinholes auto-expire after their TTL and are logged for audit.",
              style: {
                color: "#b0b8c4",
                fontSize: "13px",
                lineHeight: "1.6",
                marginBottom: "20px"
              }
            }),
            viewPinholeForm(state, dispatch, handleCreatePinhole),
            viewActivePinholes(state.activePinholes, handleRevokePinhole)
          ],
          style: {
            background: "#151a28",
            border: "2px solid #7b6cff",
            borderRadius: "12px",
            marginBottom: "16px",
            marginTop: "16px",
            padding: "24px"
          }
        }) : null,
      JsxRuntime.jsx("div", {
        children: Belt_Array.map(state.ports, port => viewPortRow(port, dispatch)),
        style: {
          marginTop: "24px"
        }
      }),
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("h4", {
            children: "⚠️ Security Best Practices",
            style: {
              color: "#f44336",
              fontSize: "16px",
              fontWeight: "700",
              marginBottom: "12px"
            }
          }),
          JsxRuntime.jsxs("ul", {
            children: [
              JsxRuntime.jsx("li", {
                children: "Never leave SSH (22), RDP (3389), or database ports open permanently"
              }),
              JsxRuntime.jsx("li", {
                children: "Use ephemeral pinholes for temporary access - they auto-close"
              }),
              JsxRuntime.jsx("li", {
                children: "All port access is logged to VeriSimDB for compliance auditing"
              }),
              JsxRuntime.jsx("li", {
                children: "Default-deny firewall rules protect all closed ports"
              }),
              JsxRuntime.jsx("li", {
                children: "Critical risk ports trigger miniKanren security warnings"
              })
            ],
            style: {
              color: "#b0b8c4",
              fontSize: "13px",
              lineHeight: "1.8",
              paddingLeft: "20px"
            }
          })
        ],
        style: {
          background: "rgba(244, 67, 54, 0.1)",
          border: "2px solid #f44336",
          borderRadius: "12px",
          marginTop: "32px",
          padding: "20px"
        }
      })
    ],
    className: "port-config-panel",
    style: {
      background: "#0a0e1a",
      minHeight: "100vh",
      padding: "32px"
    }
  });
}

let make = PortConfigPanel;

export {
  emptyPinholeForm,
  init,
  update,
  riskIndicator,
  riskColor,
  formatDuration,
  formatCountdown,
  stateLabel,
  stateColor,
  parsePinhole,
  parsePinholesResponse,
  parsePinholeResponse,
  viewPortRow,
  viewEphemeralConfig,
  inputStyle,
  labelStyle,
  viewPinholeForm,
  viewPinholeRow,
  viewActivePinholes,
  make,
}
/* react Not a pure module */
