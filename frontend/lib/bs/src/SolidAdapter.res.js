// Generated by ReScript, PLEASE EDIT WITH CARE

import * as DomMounterEnhanced from "./DomMounterEnhanced.res.js";

let SolidFFI = {};

function createDomMounter(elementId) {
  let match = createSignal(false);
  let setMounted = match[1];
  let match$1 = createSignal(undefined);
  let setError = match$1[1];
  createEffect(() => {
    let msg = DomMounterEnhanced.mount(elementId);
    if (msg.TAG === "Ok") {
      setMounted(param => true);
    } else {
      let msg$1 = msg._0;
      setError(param => msg$1);
    }
    onCleanup(() => {
      setMounted(param => false);
      setError(param => {});
    });
  });
  return {
    mounted: match[0],
    error: match$1[0]
  };
}

export {
  SolidFFI,
  createDomMounter,
}
/* No side effect */
